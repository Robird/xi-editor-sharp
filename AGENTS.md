## 跨会话记忆文档
本文档(`./AGENTS.md`)会伴随每个user消息注入你的上下文。这是一种外部辅助记忆机制，当你需要跨会话记录信息时，就记录在本文件中。当完成一个工作、制定或调整了新的计划后，务必更新本文档，以免留有错误的记忆。

## 当前项目的核心目标
- 参考位于`./reference`目录中的原始`xi-editor-core`，移植为C#实现。
- 变为类似SQLite3那样的嵌入用户进程中的执行模型，原版`xi-editor-core`的JSON RPC部分作为独立的可选实现（一个Host exe进程）。
- 移植成的C#项目统一使用dotnet 9.0大版本和xUnit测试框架。

## 进度追踪方法
1. 对照总体目标和当前进展。
2. 在待完成事项中列出接下来的计划。可以是粗粒度的，也可以是细粒度的，想到值得做的事情就先记录下来。
3. 逐步展开粗粒度的待完成事项，直到可以直接开始实施。展开事项时使用markdown的子list和缩进表示，能辅助你清晰的记录事项间的递归关系。
4. 完成一个事项后，移入`已完成事项`，这往往通过实际移动`## 已完成事项`标题来完成。

## 已完成事项
(Empty)

## 待完成事项
(Empty)