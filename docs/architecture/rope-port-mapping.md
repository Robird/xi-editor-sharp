# Rope 文件级映射与类型翻译计划

> **Scope**: Keep a compressed Rust↔C# file/type matrix plus the parity-assets ledger for Xi.Editor Rope.
> **Owner**: Architecture Mapper
> **Update Frequency**: Whenever a module state changes or a new Stage D asset lands.
> **Reviewers**: AI Architect · C# Implementer · Rust Porter · QA Engineer
> **Anchor Prefix**: RPM
> **Last Synced Goal Tree**: 2025-11-18 19:36 UTC (`goal_tree_sync.py` log `tests/xi.Core.Tests/Fixtures/Reports/goal-tree-sync-20251118-193607.log`)
<!-- goal-tree:meta generated-at="2025-11-18T19:26:55+00:00" source="docs/architecture/templates/goal-tree.yaml" checksum="7a47156ffd7e47c1f5e0a2a6f2b489b40ed9f687200e1479aab8194dde1b7df4" payload-hash="9c7163ebd6ceb7f9e7e4ec566682470e96e27424499c4a6f5804898eca48f1f0" inspector-report="tests/xi.Core.Tests/Fixtures/Reports/stage-d-inspector-latest.txt" chunk-report="tests/xi.Core.Tests/Fixtures/Reports/chunk-bench-latest.txt" telemetry-report="tests/xi.Core.Tests/Fixtures/Reports/grapheme-telemetry-latest.txt" -->

---

## [RPM-Matrix] File & Type Matrix
<a id="RPM-Matrix"></a>
| Module | Rust Source | C# Target | Status | Skeleton Coverage | Notes |
| --- | --- | --- | --- | --- | --- |
| Node / SharedNode / TreeBuilder | `rope/tree.rs` | `Tree/Node*.cs`, `TreeBuilder.cs`, `Tree/LeafSplitter.cs`, `Tree/StringLeafOperations.cs`, `Tree/TreeBuilderTracer.cs`, `Diagnostics/TreeBuilder/TreeBuilderSliceTraceLoader.cs` | Implementation (slice trace loader ready) | Rust skeleton exposes `Node<N,L>`, `SharedNode`, `TreeBuilderTracer`, `TreeBuilderEventKind`; C# ships `TreeBuilderTracer/EventKind/Event/ITreeBuilderTracer` + `NoOpTreeBuilderTracer` and now hydrates manifest-managed traces via `TreeBuilderSliceTraceLoader` + tests. | SharedNode helpers + `_editVersion` + tracer scaffolding exist, and `TreeBuilderSliceTraceLoaderTests` prove we can read the Stage D exporter output at `tree_builder_slice/basic_slice_plan.json`; Ready Queue #3 locked the exporter defaults so `tree_builder_slice_trace` is always on and manifest `metric_windows[]` records `tree_builder_trace_events=3`, leaving only the tracer injection + `[TS-B2]` MetricAdapter plumbing outstanding. |
| Cursor stack | `rope/tree.rs::Cursor` | `Tree/NodeCursor.cs`, `tests/.../CursorDescriptorParityTests.cs` | Manifest-backed ✅ (2025-11-18 run) | `fixtures.manifest.json` 记录 `cursor_descriptors@1.2.0`（count=12，payload hash `9b46bd8e29042e38c36556afc4054a5a2c4a6cfa405b5bbd738ca1909f8a4d38`，feature gates `["cursor_state","serde","tree_builder_slice_trace"]`），`tests/xi.Core.Tests/Fixtures/Reports/stage-d-inspector-latest.txt` 重复同一 ledger，QA 可以引用单一证据链。 | `_editVersion` parity 已绿且 `docs/architecture/m3-implementation-plan.md#[MP-T1]` 现引用同一 manifest/ledger；2025-11-18 20:30Z 批次的 `tests/xi.Core.Tests/Fixtures/Reports/stage-d-refresh-20251118-203039.log` + `stage-d-inspector-latest.txt` 继续作为 `[QA-IngestionSmoke]`/`[StageD::FixtureFlow]` 的证据来源，并提醒 `StageDDescriptorInspector` 仍需 `tee` 写报告。 |
| Rope core & metrics | `rope/rope.rs` | `Rope.cs`, `RopeInfo.cs`, `Metrics.cs`, `IMetric.cs` | Implementing | Rust keeps `helpers/string_leaf.rs`, `metrics/*`, `BreaksLeaf/BreaksInfo`; C# only ships `StringLeafOperations` + `BreaksMetricHelper`, missing `Breaks` node skeleton (`BreakBuilder`, `BreaksLeaf`). | `StringLeafOperations` + `BreaksMetricHelper` landed; conversion shim still mirrors Rust via `[MP-T1]`. |
| Delta / Transformer | `rope/delta.rs` | `Rope/Delta*.cs` | Implementing | Rust skeleton includes `Delta`, `DeltaElement`, `Transformer`, `SubsetFactor`; C# has `Delta<TInfo,TLeaf>`/`DeltaJson` but no `Transformer`/`Factor` chain or subset builder parity. | Stage B JSON parity lives in `[StageD::ParityAssets]`; `Transformer`/`factor` tracked under `[MP-T1]`. |
| Engine / Undo ledger | `rope/engine.rs` | `Rope/Engine*.cs` | Done | Rust engine skeleton exposes `Engine`, `Rev`, `Contents`, `UndoGroup`, `gc` helpers; C# implements `Engine`, `Revision*`, `EngineJson` but still lacks merging/gc APIs beyond serialization playback. | Stage C fixtures frozen; no open TODO beyond perf tuning. |
| Chunk + Line enumerators | `rope/rope.rs::ChunkIter`, `Lines*` | `RopeChunkEnumerator*.cs`, `RopeLineEnumerator*.cs`, `Rope/Diagnostics/Descriptors/{ChunkDescriptor,LineDescriptor,StageDDescriptorLoader}.cs` | Manifest-backed ✅ (2025-11-18 run; QA wiring pending) | Manifest + inspector ledger capture 20 chunk descriptors（`payload_hash=9c7163ebd6ceb7f9e7e4ec566682470e96e27424499c4a6f5804898eca48f1f0`）以及 11 line descriptors与 668 grapheme windows，Release chunk bench `tests/xi.Core.Tests/Fixtures/Reports/chunk-bench-latest.txt` 将同一 rust_commit=b6fb5999 证据映射到 5.51 ms chunk / 4.21 ms line（181.45/237.40 MB/s，alloc 37,944 B），因此 `[QA-IngestionSmoke]` 与 `[QA-ChunkBench]` 共用同一 artefact。 | Copy-on-read enumerators + telemetry counters remain the downgrade path; loader coverage lives in `tests/xi.Core.Tests/Diagnostics/StageDDescriptorLoaderTests.cs`，但 `[TS-B3]` + `[QA-ChunkBench]` 仍要求把该基准接入 QA 自动化，因此继续保持黄色。 |
| Grapheme navigation | `rope/rope.rs::GraphemeCursor` | `Navigation/DegradedGraphemeNavigator.cs`, `GraphemeNavigationMetrics.cs`, `Rope/Diagnostics/Descriptors/GraphemeDescriptor.cs` | Manifest-backed ✅ (2025-11-18 run; degraded impl monitored) | Manifest row `grapheme_descriptors@1.0.0` 保持 668 samples（payload hash `b3484eab43303735c754fa4fb6928255ef7dbe053d2cf41ce60a7c63a7a59095`），`tests/xi.Core.Tests/Fixtures/Reports/stage-d-inspector-latest.txt` 重复相同计数/hash，而 `[QA-Telemetry]` 同批记录 `tests/xi.Core.Tests/Fixtures/Reports/grapheme-telemetry-latest.txt` + `.trx`（12,000 ops，fallback 0%，Requires fallback 0.3000%）。 | Surrogate-safe fallback remains acceptable per `[Div-Active]`; 接下来仍需把 loader 输出接入 `[QA-Telemetry]` dashboard，让 `[TS-B3]`/`[TS-B4]` 可针对 manifest-backed payload 强制 ≤0.5% fallback 阈值。 |
| Breaks tree / soft line metrics | `rope/breaks.rs`, `rope/metrics/break_indices.rs`, `core-lib/src/linewrap.rs` | `Rope/BreaksMetricHelper.cs`, `Rope/Breaks/BreaksDescriptor.cs`, `Rope/Breaks/BreaksTree.cs`, `Rope/Breaks/BreakBuilder.cs`, `Rope/Diagnostics/Descriptors/StageDDescriptorHydrator.cs` | Manifest-backed ✅ (2025-11-18 run) | Manifest `breaks_descriptors@1.0.0` 现携带 3 个 BreakPlan 样本（payload hash `527ca4f2000b7548a10fe94cf51f6e758f23fa73e7f1ce82347ce92ab61a1f83`），Inspector 捕捉 wrap span 24–38 与样本 `ascii_guidance`、`crlf_emoji_mix`、`delimited_tables`，证明 exporter 仍在线。 | `StageDDescriptorHydrator.LoadBreakPlans` consumes the same ledger；`[TS-B5]` 继续追踪 QA ingestion + anchor updates，让 `[QA-IngestionSmoke]` / `[QA-ChunkBench]` 直接引用 inspector 证据而非 placeholder JSON。 |
| Diff / Compare | `rope/diff.rs`, `rope/compare.rs`, `rust/core-lib/src/find.rs` | `Diff/DiffCaseDescriptor.cs`, `Diff/DiffBuilder.cs`, `Diff/LineHashDiff.cs`, `Diff/DiffRegion.cs`, `Diff/DiffOperation.cs`, `Rope/Diagnostics/Descriptors/StageDDescriptorHydrator.cs` | Manifest-backed ✅ (2025-11-18 run) | Manifest + Inspector confirm 3 diff region snapshots（`payload_hash=afc04b228f19c7663f1d43597973c0afc90a34159b64ccaf7b9407e6613811a6`），每案 4 ops，因此 hydration 终于引用真实 `LineHashDiff` 数据而非 doc 占位。 | `DiffSkeletonTests` 仍留在 smoke filter；`[TS-B5]` 持续把 Inspector 日志喂给 `[QA-IngestionSmoke]` 并推广该 hash，以便 QA 拥有 Finder/Undo 回归的 typed 证据。 |
| Search / Spans | `rope/find.rs`, `rope/spans.rs` | `Search/SearchDescriptor.cs`, `Search/Finder.cs`, `Search/SearchOptions.cs`, `Search/SearchResult.cs`, `Search/SearchSpanWindow.cs`, `Search/SearchHit.cs`, `Rope/Diagnostics/Descriptors/StageDDescriptorHydrator.cs` | Manifest-backed ✅ (2025-11-18 run) | Inspector summary lists 3 span windows（`payload_hash=599f25b06a36d417cd1ca51a818c0ac5dfcf162c9530368d62cd7075c840df51`）with queries ^WARN:.*$, `stage`, and 🚀，matching the manifest ledger and proving regex + literal coverage。 | `SearchSkeletonTests` still guard DTOs; `[TS-B5]` 继续确保 `[QA-IngestionSmoke]` 引用 Stage D inspector log，并让 Finder smoke 直接消费 manifest-backed descriptors。 |

> Remaining helper/details (Subset, Interval, serde DTOs, CLI commands) live in `docs/csharp-refactor/rope-serialization-fixture-playbook.md` and `[StageD::ParityAssets]`.

## [RPM-ParityAssets] Parity Assets & Stage D Hooks
<a id="RPM-ParityAssets"></a>
| Asset | Path | Status | Source / Notes |
| --- | --- | --- | --- |
| Serde baseline (Subset/Delta/Engine) | `tests/xi.Core.Tests/Fixtures/{subset,delta,engine}_serialization/` | ✅ Current | Refreshed via `scripts/refresh_serialization_fixtures.ps1`; `dotnet test Xi.Editor.sln -v m` -> 169/169 gets attached to `[QA-IngestionSmoke]` whenever these change. |
| Cursor descriptors | `tests/xi.Core.Tests/Fixtures/cursor_descriptors/` | Manifest-backed ✅ (2025-11-18 run) | Manifest row lists `cursor_descriptors@1.2.0` `count=12`, `payload_hash=9b46bd8e29042e38c36556afc4054a5a2c4a6cfa405b5bbd738ca1909f8a4d38`, `feature_gates=["cursor_state","serde","tree_builder_slice_trace"]`; the same numbers appear in `tests/xi.Core.Tests/Fixtures/Reports/stage-d-inspector-latest.txt`, so `[TS-B1]` now focuses on QA ingestion + documentation instead of exporter readiness. |
| Chunk descriptors | `tests/xi.Core.Tests/Fixtures/chunk_descriptors/` | Manifest-backed ✅ (2025-11-18) | Latest ledger captures 20 chunk windows (`schema_hash=chunk_descriptors@1.0.0`, `payload_hash=9c7163ebd6ceb7f9e7e4ec566682470e96e27424499c4a6f5804898eca48f1f0`), `tests/xi.Core.Tests/Fixtures/Reports/stage-d-inspector-latest.txt` mirrors the same stats, and the Release chunk bench log `tests/xi.Core.Tests/Fixtures/Reports/chunk-bench-latest.txt` documents the 5.51 ms/4.21 ms replay that `[QA-ChunkBench]` cites. |
| Grapheme windows | `tests/xi.Core.Tests/Fixtures/grapheme_descriptors/` | Manifest-backed ✅ (2025-11-18) | 668 grapheme windows (`payload_hash=b3484eab43303735c754fa4fb6928255ef7dbe053d2cf41ce60a7c63a7a59095`) are recorded in both the manifest and `stage-d-inspector-latest.txt`, and `[QA-Telemetry]` archives `tests/xi.Core.Tests/Fixtures/Reports/grapheme-telemetry-latest.txt` + `.trx` from the same run for fallback monitoring (`[TS-B4]`). |
| Tree builder slice trace | `tests/xi.Core.Tests/Fixtures/tree_builder_slice/` | Manifest-backed ✅ (2025-11-18) | `basic_slice_plan.json`（3 events，hash `22724af7fe8b22e4e1dbd01f86ba1b3902a0ccf777944b5b29081259927c3a6e`, schema `tree_builder_slice_trace@1.0.0`）由 `--tree-builder-trace` 默认导出；`TreeBuilderSliceTraceLoader` + tests 现直接消费该路径，后续等待更长 CLI traces（`[TS-B2]`）。 |
| Fixture manifest ledger | `tests/xi.Core.Tests/Fixtures/fixtures.manifest.json` | Manifest-backed ✅ (2025-11-18) | `rust_commit=b6fb5999288946daeeadc4b7f9f9756f6dda1f50`, `cli_rev=0.3.0`, `feature_gates=["cursor_state","serde","tree_builder_slice_trace"]`；the same ledger, inspector log (`tests/xi.Core.Tests/Fixtures/Reports/stage-d-inspector-latest.txt`), chunk bench report (`tests/xi.Core.Tests/Fixtures/Reports/chunk-bench-latest.txt`), and telemetry report (`tests/xi.Core.Tests/Fixtures/Reports/grapheme-telemetry-latest.txt`) now co-host the canonical counts/hashes for chunk/cursor/grapheme/breaks/diff/search/serde assets, so `[StageD::ParityAssets]` 与 `[Fixture-Manifest]` can cite one source. |
| Stage D scripts | `scripts/refresh_serialization_fixtures.ps1` | ✅ Loader + hydrator + manifest verifier + Inspector | Wrapper 默认执行 `StageDDescriptorLoaderTests -> StageDDescriptorHydratorTests -> python scripts/verify_fixture_manifest.py --manifest tests/xi.Core.Tests/Fixtures/fixtures.manifest.json -> dotnet run --project tools/StageDDescriptorInspector -- --fixtures tests/xi.Core.Tests/Fixtures` 并把日志串回 `[QA-IngestionSmoke]`；`python scripts/refresh_all_assets.py --only stage-d-fixtures` 继承同一序列，除非 QA 明确记下 `-SkipManifestVerification` 或 `-SkipStageDInspector`。 |
| Breaks descriptors (soft line metrics) | `tests/xi.Core.Tests/Fixtures/breaks_descriptors/breaks_descriptors.json` | Manifest-backed ✅ (2025-11-18) | Manifest row `count=3`, `payload_hash=527ca4f2000b7548a10fe94cf51f6e758f23fa73e7f1ce82347ce92ab61a1f83`; inspector log lists wrap width span 24–38 + sample ids, giving `[TS-B5]` solid evidence for BreakPlan hydration. |
| Diff region snapshots | `tests/xi.Core.Tests/Fixtures/diff_regions/diff_regions.json` | Manifest-backed ✅ (2025-11-18) | 3 diff cases (`payload_hash=afc04b228f19c7663f1d43597973c0afc90a34159b64ccaf7b9407e6613811a6`) now appear in both manifest + inspector ledger with algorithms=LineHash, ops per case=4, so QA can cite `[QA-IngestionSmoke]` instead of doc samples. |
| Search hits & span windows | `tests/xi.Core.Tests/Fixtures/search_spans/search_spans.json` | Manifest-backed ✅ (2025-11-18) | Inspector output lists regex + literal queries (^WARN:.*$, stage, 🚀) and matches the manifest hash `599f25b06a36d417cd1ca51a818c0ac5dfcf162c9530368d62cd7075c840df51`, proving `--search-spans` now exports real payloads referenced by `[TS-B5]`. |

> `StageDDescriptorHydrator` (see `src/xi.Core/Rope/Diagnostics/Descriptors/StageDDescriptorHydrator.cs`) consumes the same `fixtures.manifest.json` ledger as `StageDDescriptorLoader` to materialize Breaks/Diff/Search samples; keep `[StageD::FixtureFlow]` in sync whenever the Rust CLI flags `--breaks-descriptors`, `--diff-regions`, or `--search-spans` change so QA tooling never parses raw JSON twice.

> **Metric windows ledger**：`StageDDescriptorManifest.MetricWindows` 直接映射 `fixtures.manifest.json.metric_windows[]`（schema 见 `docs/architecture/fixtures/parity-fixture-schema.md#[Fixture-MetricWindows]` / `[StageD::FeatureGates]`），因此 C#/QA 可在不重新读取 JSON 的情况下获知 chunk/line/grapheme/break/diff/search/tree trace 的窗口数量与版本。

## [RPM-Actions] Open Actions
<a id="RPM-Actions"></a>
1. **11/22 – QA ingestion/anchor 合并 (Owners: QA Engineer + Architecture Mapper)** – Attach the fresh manifest + `tests/xi.Core.Tests/Fixtures/Reports/stage-d-inspector-latest.txt` output to `[QA-IngestionSmoke]`, propagate the hashes into `[QA-ChunkBench]`/`[QA-Telemetry]`, and cross-link `[RPM-ParityAssets]`/`[StageD::ParityAssets]` so every anchor now cites the manifest-backed ledger (command: `python scripts/refresh_all_assets.py --only stage-d-fixtures`).
2. **11/24 – Stage D loader → CI wiring (Owners: C# Implementer + QA Engineer)** – Ensure CI invokes `python scripts/refresh_all_assets.py --only stage-d-fixtures` + loader/hydrator smoke, uploads the inspector artifact, and threads manifest-backed descriptors into `RopeChunkEnumeratorBenchmarks`; references `[TS-B3]`, `[QA-IngestionSmoke]`, `[QA-ChunkBench]`.
3. **11/27 – Documentation + anchor reconciliation (Owner: Architecture Mapper)** – Re-run the RPM/TS/Div/System sweep so `[RPM-Matrix]`, `[RPM-ParityAssets]`, `[TS-B1]`, `[TS-B3]`, `[TS-B5]`, `[Div-Active]`, `[SO-Map]`, and `[StageD::ParityAssets]` all point at the 2025-11-18 ledger; escalate via AI Architect if any anchor still lists pre-manifest placeholders.
4. **11/28 – Metric windows ingestion（Owners: C# Implementer + QA Engineer）** – Update `StageDDescriptorHydrator`/`StageDDescriptorInspector` consumers (`MetricAdapter`, `QA chunk bench`, `cursor parity` dashboards) to read `StageDDescriptorManifest.MetricWindows` and persist the counts alongside manifest hashes. Document the wiring in `[StageD::FeatureGates]` + `[QA-IngestionSmoke]`; QA to flag any future gate（`iterator_facade` 等）仍缺 `metric_windows` 条目。
5. **11/29 – Metric windows → Hydrator/MetricAdapter 落地（Owners: C# Implementer + Architecture Mapper）** – 实际将 `StageDDescriptorManifest.MetricWindows` 字段解析到 `StageDDescriptorHydrator`、`StageDDescriptorManifest` C# DTO 与 `MetricAdapter`，并在 `tests/xi.Core.Tests/Diagnostics/StageDDescriptorLoaderTests.cs` / `[TS-B2]` 增加断言，同时将消费路径记录到 `docs/sprints/sptrint-1.md#risk--next-cycle` 与 `[MP-Risks]` 所引用的 Sprint 1 风险条目中，确认 C# 侧不再依赖手写窗口计数。
6. **11/30 – Stage D CLI → QA automation（Owners: QA Engineer + Tooling）** – 将 `python scripts/refresh_all_assets.py --only stage-d-fixtures --check` 与 Stage D CLI flag 表纳入 nightly job，强制上传 `tests/xi.Core.Tests/Fixtures/Reports/stage-d-inspector-latest.txt` 与 `chunk-bench-latest.txt` 到 `[QA-IngestionSmoke]` / `[StageD::FixtureFlow]`，并在失败时阻断 Sprint 2 Intake；本项复用 Sprint 1 `Risk & Next Cycle` 行动，避免 Stage D ledger 与 Goal Tree drift。

## [RPM-ChangeLog] Change Log
<a id="RPM-ChangeLog"></a>
- **2025-11-18 – Inspector-backed parity refresh**: consumed `tests/xi.Core.Tests/Fixtures/fixtures.manifest.json` (`rust_commit=b6fb5999288946daeeadc4b7f9f9756f6dda1f50`, `feature_gates=["cursor_state","serde","tree_builder_slice_trace"]`) plus `tests/xi.Core.Tests/Fixtures/Reports/stage-d-inspector-latest.txt`, `chunk-bench-latest.txt`, and `grapheme-telemetry-latest.txt` to mark Cursor/Chunk/Grapheme/Breaks/Diff/Search rows as “Manifest-backed ✅ (2025-11-18 run)” and rewrote `[RPM-ParityAssets]`/`[RPM-Actions]` accordingly.
- **2025-11-20 – Stage D manifest sync**: lifted chunk/grapheme/breaks/diff/search hashes + ledger metadata to the 2025-11-19 manifest (`rust_commit=bea8a3360131a0840d26aa75daba9184b70d50b7`, `feature_gates=["serde"]`) and documented the Inspector CLI as part of the loader -> hydrator -> manifest verifier -> Inspector evidence chain referenced by `[QA-IngestionSmoke]`/`[StageD::FixtureFlow]`.
- **2025-11-18 – Template rollout**: collapsed the full matrix to the ten critical modules, pushed the rest into Stage D references, added `[RPM-ParityAssets]`/`[RPM-Actions]` anchors, and recorded cursor/chunk/grapheme asset status per `[TS-B1]`/`[TS-B3]`/`[TS-B4]`.
- **2025-11-17 – Diagnostics reminder**: captured `_editVersion` + telemetry notes (legacy entry, retained for context).

[BP-GoalTree]: port-blueprint.md#bp-goaltree
[TS-B1]: type-system-migration-log.md#ts-b1
[TS-B2]: type-system-migration-log.md#ts-b2
[TS-B3]: type-system-migration-log.md#ts-b3
[TS-B4]: type-system-migration-log.md#ts-b4
[TS-B5]: type-system-migration-log.md#ts-b5
[MP-T3]: m3-implementation-plan.md#23-任务-3chunk-迭代器骨架
[Div-Active]: design-divergence-log.md#div-active
[QA-ChunkBench]: ../csharp-refactor/rope-serialization-fixture-playbook.md#QA-ChunkBench
[QA-Telemetry]: ../csharp-refactor/rope-serialization-fixture-playbook.md#QA-Telemetry
[QA-IngestionSmoke]: ../csharp-refactor/rope-serialization-fixture-playbook.md#QA-IngestionSmoke
[StageD::ParityAssets]: ../csharp-refactor/rope-serialization-fixture-playbook.md#StageD::ParityAssets
[StageD::FeatureGates]: ../csharp-refactor/rope-serialization-fixture-playbook.md#StageD::FeatureGates
[StageD::FixtureFlow]: ../csharp-refactor/rope-serialization-fixture-playbook.md#StageD::FixtureFlow
[Fixture-Manifest]: fixtures/parity-fixture-schema.md#fixture-manifest
[Chat-2025-11-20]: ../meetings/2025-11-20-type-mapping-sync-chat.md#architecture-mapper
